<h1>Some Burguers</h1>

<h2>This are the burguers in queue!</h2>

<ul>
  {{#each burguers}}
    {{#unless uActive}}
      {{> burguers/burguer-block uActive=false}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These are the burguers being eaten!</h2>

<ul>
  {{#each burguers}}
    {{#if uActive}}
      {{> burguers/burguer-block uActive=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Order a Burguer</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burguer Name:</label>
    <input type="text" id="ca" name="uName">
  </div>
  <button type="submit">Add Burguer</button>
</form>
